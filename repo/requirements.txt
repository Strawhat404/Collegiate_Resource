PyQt6>=6.5
cryptography>=41
rapidfuzz>=3.0
openpyxl>=3.1
# Test-only dependencies (the headless suite skips these gracefully when
# they are not installed, but CI for the UI smoke suite requires them):
pytest>=7
pytest-qt>=4
